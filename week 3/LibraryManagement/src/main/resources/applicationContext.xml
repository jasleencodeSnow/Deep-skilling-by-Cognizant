<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
               http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
               http://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/aop
               http://www.springframework.org/schema/aop/spring-aop.xsd">

    <!--
        =========================================================
        Exercise 1 & 5: Basic bean definitions for Spring IoC
        =========================================================
    -->

    <!-- BookRepository bean -->
    <bean id="bookRepository" class="com.library.repository.BookRepository"/>

    <!--
        =========================================================
        Exercise 2 & 5: Setter injection — wire BookRepository into BookService
        =========================================================
        Exercise 7: Setter Injection configured here
    -->
    <bean id="bookServiceSetter" class="com.library.service.BookService">
        <property name="bookRepository" ref="bookRepository"/>
    </bean>

    <!--
        =========================================================
        Exercise 7: Constructor Injection
        =========================================================
    -->
    <bean id="bookServiceConstructor" class="com.library.service.BookService">
        <constructor-arg ref="bookRepository"/>
    </bean>

    <!--
        =========================================================
        Exercise 6: Component scanning for annotation-based config
        (@Service, @Repository, @Autowired in the classes themselves)
        =========================================================
    -->
    <context:component-scan base-package="com.library"/>

    <!--
        =========================================================
        Exercise 3 & 8: Enable AspectJ auto-proxying for AOP
        =========================================================
    -->
    <aop:aspectj-autoproxy/>

    <!--
        Exercise 8: Register the LoggingAspect bean
        (component-scan above also picks it up via @Aspect + @Component,
         but explicit bean declaration is shown here for clarity)
    -->
    <!-- <bean id="loggingAspect" class="com.library.aspect.LoggingAspect"/> -->

</beans>
